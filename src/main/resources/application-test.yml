spring:
  application:
    name: loanmanagement
  main:
    banner-mode: off
  datasource:
    url: jdbc:postgresql://localhost:5432/postgresql
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
